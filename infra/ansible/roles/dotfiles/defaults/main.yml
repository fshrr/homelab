---
dotfiles_user: "{{ admin_user | default(ansible_user) }}"
dotfiles_home: "/home/{{ dotfiles_user }}"
dotfiles_repo: https://github.com/fshrr/dotfiles
dotfiles_version: main
# Passed to install.sh. server | minimal | skip
dotfiles_profile_name: "{{ dotfiles_profile | default('server') }}"
