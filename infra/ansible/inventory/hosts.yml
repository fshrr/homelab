---
all:
  children:
    t3code:
      hosts:
        t3chatbox:
          # MagicDNS name: reachable from anywhere on the tailnet, survives DHCP changes.
          ansible_host: t3chatbox.fenrir-cardassian.ts.net
          ansible_user: fahim
