volumes:
  jellyfin-config:

services:
  jellyfin:
    image: lscr.io/linuxserver/jellyfin:latest
    container_name: jellyfin
    extends:
      file: ../base.yml
      service: base
    environment:
      - JELLYFIN_PublishedServerUrl=https://jellyfin.fahimshahreer.com
    volumes:
      - jellyfin-config:/config
      - ${MOVIES_PATH}:/media/movies
      - ${MOVIES_4k_PATH}:/media/movies_4k
      - ${SHOWS_PATH}:/media/shows
      - ${SHOWS_ANIME_PATH}:/media/shows_anime
    devices:
      - /dev/dri/renderD128:/dev/dri/renderD128
    ports:
      - 8096:8096
      #- 8920:8920 #for https
      - '7359:7359/udp' # for network discovery like smart TVs, media players, apps, LAN
      - '1900:1900/udp' # for TV/media devices that use DLNA, lets Jellyfin act as a DLNA server
    restart: unless-stopped
    labels:
      - homepage.group=Media
      - homepage.name=Jellyfin
      - homepage.icon=jellyfin.png
      - homepage.showStats=true
      - homepage.href=http://bromine.fenrir-cardassian.ts.net:8096
      - "traefik.enable=true"
      - "traefik.http.routers.jellyfin.rule=Host(`jellyfin.fahimshahreer.com`)"
      - "traefik.http.routers.jellyfin.entrypoints=websecure"
      - "traefik.http.services.jellyfin.loadbalancer.server.port=8096"
      - "traefik.docker.network=traefik"
